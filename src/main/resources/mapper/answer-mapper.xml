<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 해당 파일에 모든 쿼리문을 작성 -->
<!-- namespace에는 xml파일과 연결할 Mapper interface 파일명을 패키지를 포함하여 적어주세요 -->
<mapper namespace="com.green.backend_root.answer.mapper.AnswerMapper">
	<resultMap id="answer" type="com.green.backend_root.answer.dto.AnswerDTO">
        <id     column="ANS_ID" property="ansId" />
        <result column="ANS_CONTENT" property="ansContent" />
        <result column="QST_ID" property="qstId" />
        <result column="USER_ID" property="userId" />
        <result column="ANS_DATE" property="ansDate" />
    </resultMap>

    <!-- 답변 등록 -->
    <insert id="regAns">
        INSERT INTO ANSWER (
            ANS_CONTENT
            , QST_ID
            , USER_ID
        ) VALUES (
            #{ansContent}
            , #{qstId}
            , #{userId}
        )
    </insert>

    <!-- 답변 조회 -->
    <select id="getAns" resultMap="answer">
        SELECT ANS_ID
            , ANS_CONTENT
            , QST_ID
            , USER_ID
            , ANS_DATE
        FROM ANSWER
        WHERE QST_ID = #{qstId}
    </select>
</mapper>